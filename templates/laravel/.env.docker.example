# Salin jadi .env.docker  ->  copy .env.docker.example .env.docker
# Lalu jalankan: docker compose --env-file .env.docker up -d
# Atau cukup rename jadi .env kalau proyek belum punya .env Laravel

APP_NAME=laravel
APP_PORT=8000
VITE_PORT=5173
