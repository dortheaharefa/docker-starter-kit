# Salin jadi .env  ->  copy .env.example .env
# File .env tidak ikut ke repository

DB_VERSION=11.4
DB_CONTAINER=devnet-db

# Port di Windows untuk Navicat. 3307 dipakai agar tidak bentrok dengan XAMPP.
DB_PORT_HOST=3307

DB_NAME=devdb
DB_USER=dev
DB_PASSWORD=dev
DB_ROOT_PASSWORD=root
